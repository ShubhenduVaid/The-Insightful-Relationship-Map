{
  "name": "personal-strategy-engine",
  "version": "1.0.0",
  "description": "Zero-knowledge personal relationship mapping and social network analysis tool",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm run dev:server\" \"pnpm run dev:client\"",
    "dev:client": "pnpm --filter strategy-engine-client dev",
    "dev:server": "pnpm --filter strategy-engine-server dev",
    "build": "pnpm --filter strategy-engine-client build && pnpm --filter strategy-engine-server build",
    "test": "pnpm --filter strategy-engine-client test && pnpm --filter strategy-engine-server test",
    "test:coverage": "pnpm --filter strategy-engine-client test:coverage && pnpm --filter strategy-engine-server test:coverage",
    "lint": "pnpm --filter strategy-engine-client lint && pnpm --filter strategy-engine-server lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "author": "Shubhendu Vaid",
  "license": "MIT"
}
